import { LightningElement } from 'lwc';
import googlePlay from '@salesforce/resourceUrl/googlePlay';
import phoneIcon from '@salesforce/resourceUrl/phoneIcon';
import apple from '@salesforce/resourceUrl/apple';

export default class Bankpagefooter extends LightningElement {
    googlePlay = googlePlay;
    phoneIcon = phoneIcon;
    apple = apple;
}