import { LightningElement } from 'lwc';
import cardImage from '@salesforce/resourceUrl/standardBankCard';

export default class Bankpagebody extends LightningElement {
    cardImage = cardImage;
    
}